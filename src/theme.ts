import { createMuiTheme } from '@material-ui/core';

export const theme = createMuiTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#0081cb',
    },
    secondary: {
      main: '#02020a',
    },
    background: {
      default: '#333646',
    },
  },
});
